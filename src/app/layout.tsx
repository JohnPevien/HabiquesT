import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});
export const metadata: Metadata = {
  title: "Next Starter — Production-ready Next.js 16 + React 19",
  description:
    "Opinionated, production-ready starter for Next.js 16 + React 19. TDD, accessibility, Tailwind 4, shadcn/ui, Vitest, Playwright + axe, and Git hooks — already wired. Clone, pnpm dev, ship.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="flex min-h-full flex-col">{children}</body>
    </html>
  );
}
